# {프로젝트명}

## 프로젝트 컨텍스트

- 개요: {한 줄 설명}
- 기술 스택: {언어, 프레임워크, DB}
- 아키텍처: {모노레포 / 마이크로서비스 / 단일 앱}

## 주요 명령어

- 빌드: `{빌드 명령}`
- 테스트: `{테스트 명령}`
- 린트: `{린트 명령}`

## 디렉터리 구조

```
{프로젝트 주요 디렉터리 구조}
```

## 코딩 컨벤션

- import 순서: {외부 → 내부 → 타입}
- 네이밍: {camelCase / snake_case / PascalCase}
- 컨벤션 문서: `{docs/conventions.md 또는 해당 없음}`

## 작업 규칙

- `/plan`으로 계획 수립 후 구현 (3개 이상 파일 변경 시 필수)
- 구현 완료 후 `/code-review`로 리뷰
- `/commit`으로 Conventional Commits 형식 커밋
- 한 세션 = 한 작업 (컨텍스트 50% 넘으면 새 세션)

## 환경 참고

- OS: {Windows / macOS / Linux}
- 셸: {bash / zsh / powershell}
- 패키지 매니저: {npm / pnpm / yarn / pip / cargo}
